# Production origin is recorded in July 26 release evidence; verify the current
# provider mapping before use. This names-only example is intentionally blank.
VITE_API_URL=

# Development-only flag; production must not enable mock authentication.
VITE_MOCK_AUTH=

# Optional public link back to the approved Emerson Empire site.
VITE_EMPIRE_URL=
